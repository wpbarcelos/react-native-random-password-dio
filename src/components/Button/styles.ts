import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttonContainer: {
        backgroundColor: "lightblue",
        padding: 10,
        borderRadius: 4,
        margin: 5
    },
    buttonText: {
        fontWeight: 'bold',
        textAlign: 'center'
    }
})